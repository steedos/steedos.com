---
title: 组件
description: 组件是微页面的基本元素，微页面中不但有非常丰富的组件可以直接使用，同时还支持自定义组件。
---

## Amis原生组件

几乎所有的amis原生组件都适用于华炎魔方微页面，请参阅 [Amis 组件](https://aisuda.bce.baidu.com/amis/zh-CN/components)。

## 华炎魔方组件

我们为 amis 扩展了以下组件用于快速实现华炎魔方各种界面能力。

- [对象表单 ObjectForm](/docs/amis/object_form)
- [对象列表 ObjectListView](/docs/amis/object_listview)
- [对象表格 ObjectTable](/docs/amis/object_table)
- [标题面板 RecordDetailHeader](/docs/amis/record_detail_header)
- [相关列表 RecordDetailRelatedList](/docs/amis/record_detail_related_list)


## @data.change广播事件

在amis广播事件可以自定义派发的事件名称`eventName`，其他渲染器可以监听自定义事件并配置响应动作。[了解更多广播事件](https://aisuda.bce.baidu.com/amis/zh-CN/docs/concepts/event-action)。

华炎魔方基于amis广播事件，实现了基于对象的`@data.change`事件，只需要在组件中按规范监听相关对象的`@data.change`事件，可以很方便的实现刷新**对象列表**和**表单数据**功能。 


### 名称规范

事件名称规范：`@data.changed.对象的API名称`，表示触发某个对象的`@data.change`事件。

### 触发广播

要触发对象的`@data.change`事件，`eventName`按上述名称规范，并配置`actionType: 'broadcast'`。  

以下示例演示了点击按钮时触发一个名为`instance_tasks`的对象的`@data.change`事件。`actions.data`中的数据会传递给目标事件，可通过`event.data.xxx`直接获取。

```json
  {
    "type": "button",
    "label": "广播一下",
    "onEvent": {
      "click": {
        "actions": [
          {
            "actionType": "broadcast",
            "args": {
              "eventName": "@data.changed.instance_tasks"
            },
            "data":{
              "objectName": "instance_tasks"
            }
          }
        ]
      }
    }
  }
```

### 监听广播

要监听`@data.change`事件，事件名按上述名称规范即可。

以下示例演示了一个监听名为`instance_tasks`的对象的`@data.change`事件。会监听其他广播的自定义事件并执行响应动作。传递的数据通过`event.data.xxx`直接获取。

```json
  "onEvent": {
    "@data.changed.instance_tasks": {
      "actions": [
        {
          "actionType": "toast",
          "args": {
            "msgType": "info",
            "msg": "传递的数据：${event.data.objectName}",
            "position": "top-center"
          }
        }
      ]
    }
  }
```

### 组件功能

华炎魔方很多内置组件都监听了对象的`@data.change`事件，基于amis的广播事件特性可以很方便的调用这些组件的功能和行为，比如组件的刷新功能。 


#### 对象列表

[此组件的监听广播代码](https://github.com/steedos/steedos-widgets/blob/eab0501ff2624e9dc51b8a96dd6d8e3afb774c48/packages/@steedos-widgets/amis-object/src/amis/AmisObjectListview.tsx#L82)。  

对象列表监听了`@data.change`事件，会执行以下行为：  

- 刷新
- 进入记录详情页

##### **刷新**
在对象列表中编辑并保存记录后，会刷新对象列表，就是此行为实现的。  

以下示例演示点击按钮后，广播事件响应的动作会执行刷新对象列表命令。

* **data的参数**
  * objectName: `String`。对象Api名称。
  * recordId: `String`。当前操作记录的`recordId`，该值有效，进行刷新操作；无效，会进入详细页面，且需配置`data.result.data.recorId`，会根据此值进行路由跳转。选填。

```json
{
  "type": "button",
  "label": "广播事件-刷新对象列表",
  "onEvent": {
    "click": {
      "actions": [
        {
          "actionType": "broadcast",
          "args": {
            "eventName": "@data.changed.project"
          },
          "data": {
            "objectName": "project",
            "recordId": "xxx" // 此处请保持值recordId值有效，否则将会进入记录详情页
          }
        }
      ]
    }
  }
}
```

##### **进入记录详情页**

在对象列表中新建后，会进入记录详情页，就是此行为实现的。  

以下示例演示点击按钮后，广播事件响应的动作会进入记录详情页。

* **data的参数**
  * objectName: `String`。对象Api名称。
  * recordId: `String`。当前操作记录的`recordId`，该值无效，会进入详细页面，需配置`data.result.data.recorId`，会根据此值进行路由跳转；有效，进行刷新操作。选填。

```json
{
  "type": "button",
  "label": "广播事件-进入记录详情页",
  "onEvent": {
    "click": {
      "actions": [
        {
          "actionType": "broadcast",
          "args": {
            "eventName": "@data.changed.project"
          },
          "data": {
            "objectName": "project",
            "result":{
              "data":{
                "recorId": "xxx"
              }
            }
          }
        }
      ]
    }
  }
}
```


#### 对象详细页面
[此组件的监听广播代码](https://github.com/steedos/steedos-widgets/blob/eab0501ff2624e9dc51b8a96dd6d8e3afb774c48/packages/@steedos-widgets/amis-lib/src/lib/converter/amis/index.js#L426)。  
对象详细页面监听了`@data.change`事件，会执行以下行为：  
- 刷新
- 进入对象列表


##### **刷新**

在对象详细页面编辑后，会刷新页面，就是此行为实现的。

以下示例演示了点击按钮后，广播事件响应的动作会执行刷新对象详细页面命令。

* **data的参数：**
  * objectName: `String`。对象Api名称。
  * __deletedRecord: `Boolean`。是否为删除操作，若为真，则跳转到对象列表。选填。


```json
{
  "type": "button",
  "label": "广播事件-刷新纪录页",
  "onEvent": {
    "click": {
      "actions": [
        {
          "actionType": "broadcast",
          "args": {
            "objectName": "project",
            "eventName": "@data.changed.project"
          },
          "data": {
            "objectName": "project"
          }
        }
      ]
    }
  }
}
```

##### **进入对象列表**

在对象详细页面删除记录后，会刷新页面，就是此行为实现的。

以下示例演示了点击按钮后，广播事件响应的动作会进入对象列表。

* **data的参数：**
  * objectName: `String`。对象Api名称。
  * __deletedRecord: `Boolean`。是否为删除操作，若为真，则跳转到对象列表。选填。

```json
{
  "type": "button",
  "label": "广播事件-进入对象列表",
  "onEvent": {
    "click": {
      "actions": [
        {
          "actionType": "broadcast",
          "args": {
            "objectName": "project",
            "eventName": "@data.changed.project"
          },
          "data": {
            "objectName": "project"
          }
        }
      ]
    }
  }
}
```



#### 对象相关表列表

[此组件的监听广播代码](https://github.com/steedos/steedos-widgets/blob/eab0501ff2624e9dc51b8a96dd6d8e3afb774c48/packages/@steedos-widgets/amis-lib/src/lib/objectsRelated.js#L161)。  

对象相关表列表进行增、删、改后，会刷新页面，就是此行为实现的。

以下示例演示了点击按钮后，广播事件响应的动作刷新对象相关表列表。

* **data的参数：**
  * objectName: `String`。对象Api名称。

```json
{
  "type": "button",
  "label": "广播事件-刷新相关表",
  "onEvent": {
    "click": {
      "actions": [
        {
          "actionType": "broadcast",
          "args": {
            "objectName": "project",
            "eventName": "@data.changed.project" // 触发广播后刷新project相关表
          },
          "data": {
            "objectName": "project"
          }
        }
      ]
    }
  }
}
```



## 自定义组件

[Amis](https://aisuda.bce.baidu.com/amis/zh-CN/docs) 本身提供了非常丰富的组件功能，基本它可以实现大部分业务场景的UI界面需求。

如果遇到适合自己开发自定义组件的场景，我们也可以通过自定义资产包来把自定义组件集成到amis设计器中，这样我们就可以实现任何我们想要的UI效果了。 

只要把自定义资产包地址配置到环境变量 `STEEDOS_PUBLIC_PAGE_ASSETURLS` 中，华炎魔方会根据资产包中配置的资源包地址自动把相关自定义组件加载并集成到amis引擎中，非常方便。

```bash
STEEDOS_PUBLIC_PAGE_ASSETURLS=https://unpkg.com/@steedos-widgets/example@0.0.6/dist/assets.json
```

资产包的地址只要可以访问即可，它可以是发布到npm的包(unpkg.com)地址，也可以是其他任何可以被当前服务访问的地址，比如在远程开发环境中公开的资产包地址。

下面的示例 [amis-custom-components](/docs/amis/example#amis-custom-components) 完整演示了如何在华炎魔方项目中通过引用自定义资产包的方式来集成自定义组件。

关于自定义资产包及自定义组件的开发，请参考下面的示例 [steedos-widgets](/docs/amis/example#steedos-widgets)。
