---
title: 绑定云服务
description: 绑定云服务后，支持从应用市场下载和安装软件包。社区版可选绑定云服务，企业版必须绑定云服务。
---

## 平台初始化

请参考 [平台初始化配置](/docs/deploy/steedos-config#平台初始化) 在本地初始化魔方项目，项目初始化完成后会创建一个企业，其ID值就是我们要绑定云服务的“魔方ID”。

## 注册账户

请进入华炎魔方客户门户（<https://console.steedos.cn/>），并登录进系统。如果没有账户，请先注册。

### 新建我的魔方

在管理控制台 - 我的魔方中，新建我的魔方。

新建时一定要填写“魔方ID”属性值为项目初始化时创建的企业ID。

### 新建私有部署

选择我的魔方，在详情页，点击私有部署处的“新建”按钮，即可新建一条私有部署记录。

创建的私有部署记录中，包含魔方ID和 API Key，需在后续步骤中，配置为私有部署中的环境变量。

* 魔方ID
* 私有部署的API Key

### 配置云服务绑定参数

配置以下环境变量再重启服务，即可实现本地魔方项目与云服务绑定。

```
STEEDOS_CLOUD_SPACE_ID=#魔方ID
STEEDOS_CLOUD_API_KEY=#私有部署的API Key
```

如果需要软件包初始化，请配置以下参数：

```
STEEDOS_INITIAL_PACKAGES=@steedos-labs/oa@2.1,@steedos-labs/project # 系统启动时自动安装的软件包
STEEDOS_CLOUD_SYNC_PACKAGES=true # 是否从云端同步已购买软件包，安装最新版
```

您还可以联系我们购买 [华炎魔方企业版](/platform/pricing)。